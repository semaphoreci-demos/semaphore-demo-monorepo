version: v1.0
name: 'Stage ${{parameters.ENVIRONMENT}}'
agent:
  machine:
    type: e1-standard-2
    os_image: ubuntu1804
queue:
  name: '${{parameters.ENVIRONMENT}}-queue'
  scope: project
blocks:
  - name: Deploy
    task:
      jobs:
        - name: Stage
          commands:
          - echo Yes

      secrets:
        - name: heroku-tomfern
